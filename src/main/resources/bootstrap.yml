spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
      active: dev
  application:
    name: subscription
  
  cloud:
    config:
      uri: http://config:8888
      name: subscription
      label: dev # 가져올 Config의 Gitlab Branch Name